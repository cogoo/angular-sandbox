import { CardFlipDirective } from './card-flip.directive';

describe('CardFlipDirective', () => {
  it('should create an instance', () => {
    const directive = new CardFlipDirective();
    expect(directive).toBeTruthy();
  });
});
